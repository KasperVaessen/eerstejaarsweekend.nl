{
  "name": "eerstejaarsweekend.nl",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "build": "mkdir -p dist && cp src/index.html dist/ && cp src/*.js dist/ && tailwindcss -i ./src/styles.css -o ./dist/dist/styles.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kasper Vaessen",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.7"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.7"
  }
}
